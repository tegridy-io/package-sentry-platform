applications:
  - grafana-operator

parameters:
  components:
    grafana-operator:
      url: https://github.com/tegridy-io/component-grafana-operator.git
      version: master

  grafana_operator:
    namespace:
      name: efk-operator-grafana

    operator:
      grafana:
        enabled: true
      agent:
        enabled: true
